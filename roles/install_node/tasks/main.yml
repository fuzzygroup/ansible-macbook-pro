---
- name: install_node
  homebrew: name=node state=present
  
# - name: install_nvm
#   homebrew: name=nvm state=present
  
- name: install_nvm
  npm: name=nvm state=present
  
# npm install -g bozon
- name: install_bozon 
  npm: name=bozon state=present

# npm install electron --save-dev
- name: install_electron
  npm: name=electron state=present

#  npm install electron-positioner --save-dev
- name: install_electron-positioner
  npm: name=electron-positioner state=present

# npm install electron-devtools-installer
- name: install_electron-devtools-installer
  npm: name=electron-devtools-installer state=present

#  npm install bozon --save nconf
- name: install_nconf 
  npm: name=bozon state=present

#  npm install -g electron-cli
- name: install_electron-cli
  npm: name=electron-cli state=present

# npm install -g electron-forge
- name: install_electron-forge
  npm: name=electron-forge state=present

#  npm install -g electron-packager
- name: install_electron-packager
  npm: name=electron-packager state=present
  
#    # npm install -g react-native-cli
- name: install_react_native_cli
  npm: name=react-native-cli state=latest global=yes

#    #npm install -g electron-prebuilt
- name: install_electron-prebuilt
  npm: name=electron-prebuilt state=present
  
# brew install Caskroom/cask/electron
- name: brew install Caskroom/cask/electron
  homebrew_cask: name=Caskroom/cask/electron state=present

